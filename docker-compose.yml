services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  app:
    image: python:3.12-alpine
    working_dir: /app
    volumes:
      - ./app:/app
    command: sh -c "pip install redis && python main.py"
    depends_on:
      - redis
    stdin_open: true
    tty: true
